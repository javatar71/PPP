# Chapter 2
### Important things
OBJECT - is a region of memory.
TYPE - is a what kind of data contains in object, and what operations can be applied to that data.
VARIABLE - is a named object.

DECLARATION - giving name and type to an object.
DEFINITION - DECLARATION which allocating memory for it.
INITIALIZATION - giving initial value to a variable.
ASSIGMENT - giving new value to a variable.
INITIALIZER - initial value setting to a variable.
>This part I wrote on material, which understood from second chapter, but this defitions should be corrected. DECLARAION - means giving identifier(name) and type to the programm. Another words, u tell to compiler or computer, that "This variable with given type just exists!". DEFINITION is actually DECLARATION which `causes storage to be stored for that object` (direct quote from C standard). With initialization almost the same, as I described above.

In cin sequence of values separated with whitespaces.
Variables can only contains letters, digits, and underscores, however one can't starts from digit. Variables are case-sensitive.
Theres keywords such as else, if, int, etc, which cant't be used as variable names.
Object with type string besides sequence of chars, contains it's length.

### Conclusion
This chapter describes fundamental principles of input, types and objects. Important part dedicated for correct use of variables names and type-safety.

